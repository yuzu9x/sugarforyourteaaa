<!DOCTYPE html>
<html lang="en">
  <!--- base.njk ---> 
<head>
    <meta charset="UTF-8">

    <style>
    @font-face {
    font-family: 'MainFont';
    src: url("{{ '/assets/fonts/MainFont.ttf' | prefixedUrl }}") format('truetype');
    font-weight: normal;
    font-style: normal;
    }
    </style>

    <title>{{ title }}</title>
    <link rel="stylesheet" href="{{'/styles/style.css' | prefixedUrl }}">
    
    </head>
   <body>
  {% include "nav.njk" %}

 
  

  <h1 class="h1main">{{ title }}</h1>
  <img src="{{ '/assets/images/sugarcubes.jpg' | prefixedUrl }}" alt="sugar cubes" 
    width="400"
    height="200"
    style="margin: 0px 10px; "
    >
  
  <main>{{ content | safe }}</main>
 


<ul class="posts-list">
	<!-- loops through all of our md files with the tag 'post' --> 
  {% for post in collections.post %}
  <li><a href="{{ post.url | prefixedUrl}}">{{ post.data.title }}</a>  </li>
  {% endfor %}
</ul>

 

 {% include "footer.njk" %}

</body>

</html>